{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Transcrypt",
      "type": "shell",
      "command": "python -m transcrypt -b -n clientlibrary",
      "options": {
        "cwd": "${workspaceFolder}/app/static"
      }
    }
  ]
}
